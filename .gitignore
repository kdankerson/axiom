# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python / sidecar
.venv
__pycache__
*.pyc
sidecar/build
sidecar/dist

# Rust / Tauri
src-tauri/target
src-tauri/gen
# Tracked as a placeholder (.gitkeep) so `cargo build`/`tauri dev` succeeds
# even before scripts/build-sidecar.ps1 has run once — bundle.resources
# existence is validated at build time regardless of dev/release profile.
src-tauri/sidecar-bin/*
!src-tauri/sidecar-bin/.gitkeep
